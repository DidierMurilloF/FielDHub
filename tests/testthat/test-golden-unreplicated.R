# Golden snapshots of the unreplicated designs in the catalogue (helper-catalogue.R).
# An intended change to a design's output is accepted with
# testthat::snapshot_accept("golden-unreplicated").
test_golden_family("unreplicated")
