# Golden snapshots of the allocation designs in the catalogue (helper-catalogue.R).
# An intended change to a design's output is accepted with
# testthat::snapshot_accept("golden-allocation").
test_golden_family("allocation")
